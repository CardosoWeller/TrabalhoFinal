package com.example.trafinal.Managers

import com.example.trafinal.Models.User

class UserManager{

    /*companion object {
        protected fun registedUser(user: User) {

        }

        protected fun searcherUser(userId: Int): User {
            val user: User
            user = null
            return user
        }

        protected fun loginUser(user: User): Boolean {
            if () {
                return true
            }
            return false
        }
    }*/
}